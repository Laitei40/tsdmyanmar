# Dependencies
node_modules/

# Build outputs
dist/
admin/dist/

# Wrangler local state (D1 databases, R2 objects, temp files)
.wrangler/

# Local D1 database state (if copied outside .wrangler)
v3/

# D1 export dumps (point-in-time backups, not source — keep locally, don't version)
scripts/admin/backup-*.sql

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
*.swp

# Env files
.env
.env.local
.dev.vars
