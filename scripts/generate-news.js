// Node-based generator removed — site is static-only per project policy.
// If you need automated static HTML generation, use a GitHub Action or a simple Python script
// that runs in CI and commits generated pages back to the repo. This file intentionally
// contains no runtime code to avoid a local Node dependency.

